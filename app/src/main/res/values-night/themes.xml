<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_primary_dark</item>
        <item name="colorPrimaryDark">@color/color_primary_dark_night</item>
        <item name="colorAccent">@color/color_accent_night</item>

        <item name="colorAccentedSurface">@color/color_accent_night</item>
        <item name="colorIcon">@color/color_gray_dark</item>
        <item name="colorDivider">@color/color_gray_dark</item>
        <item name="android:statusBarColor">@color/color_primary_dark_night</item>
    </style>
</resources>